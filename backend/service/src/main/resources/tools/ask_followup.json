{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://interviewer.org/tools/ask_followup.json",
  "title": "ask_followup",
  "description": "Ask a follow-up question about the answer the candidate just gave. Use to probe a vague, shallow, or unusually interesting answer.",
  "type": "object",
  "properties": {
    "question": {
      "type": "string",
      "maxLength": 400
    },
    "rationale": {
      "type": "string",
      "maxLength": 300,
      "description": "Why this probe, in one sentence."
    },
    "parentQuestionId": {
      "type": "string",
      "description": "The questionId returned by the fetch_question call this follows up on."
    },
    "competency": {
      "type": "string",
      "enum": ["correctness", "depth", "communication", "practical_experience"]
    }
  },
  "required": ["question", "parentQuestionId"],
  "additionalProperties": false
}
